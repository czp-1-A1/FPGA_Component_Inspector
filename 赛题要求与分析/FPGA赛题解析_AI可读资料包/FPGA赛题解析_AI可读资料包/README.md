# AI可读资料包

这个资料包由原始PPT转换而来，目标是尽量做到**信息不丢失、差异不擅自纠正、结构便于AI读取**。

建议读取顺序：
1. `content.json` —— 最完整的结构化入口；
2. `content.md` —— 更适合人和LLM直接阅读的逐页转写；
3. `assets/` —— 复核流程图、示例图、开发板/摄像头照片；
4. `original.pptx` —— 最终权威原始文件。

特别说明：第4页和第8页的LUT数量不一致（38,189 vs 38184），资料包**不做纠正**，两处均保留。
